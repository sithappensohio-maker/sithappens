# ── IDE / editor metadata ───────────────────────────────────────────
.idea/
.vscode/
.claude/
.DS_Store

# ── Dependencies ────────────────────────────────────────────────────
node_modules/
.yarn/install-state.gz

# ── Build / cache artefacts ─────────────────────────────────────────
build/
dist/
.cache/
*.tsbuildinfo
frontend/node_modules/.cache/

# ── Python ──────────────────────────────────────────────────────────
__pycache__/
*.pyc
venv/
.venv/
.venv_local_test/
.ruff_cache/
.pytest_cache/
backend/_local_test_backups/
backend/_local_backend_stdout.log
backend/_local_backend_stderr.log
frontend/_local_frontend_stdout.log
frontend/_local_frontend_stderr.log
frontend/_start_local_frontend.ps1

# ── Logs ────────────────────────────────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── Environment / secrets — NEVER commit these ──────────────────────
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
*token.json*
*credentials.json*
*.env
.credentials

# ── Local backups & data dumps ──────────────────────────────────────
backups/
*.tar
*.tar.gz
*.tgz
*.zip
sit-happens-users-*.json
sit-happens-backup-*.json
sit-happens-backup-*.tar.gz
sit-happens-FULL-BACKUP-*.tar.gz

# ── Local testing artifacts ───────────────────────────────────────
test_reports/
test_result.md

# ── Incident evidence archives — raw data dumps, never source ───────
backend/incident_reports/
