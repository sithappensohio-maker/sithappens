# Rewards Phase 5B — real referral cleanup

- Rewards Center referral rows now only show real referral-code matches.
- Imported placeholder/junk `referred_by_code` values are hidden from the reward table and counted in the summary instead of being treated as ready referrals.
- Self-referral values are hidden and counted separately.
- Settings → Rewards & Referrals now has a direct Rewards Center link and clearer wording that settings configure rules while the Rewards sidebar page manages pending rewards.
- No client/dog/booking/credit data is deleted or recalculated.
