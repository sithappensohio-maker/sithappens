"""Business-domain packages for the Sit Happens backend.

New backend work belongs in a domain package rather than growing server.py.
server.py remains the compatibility/application composition root while legacy
routes are extracted incrementally behind stable /api URLs.
"""
